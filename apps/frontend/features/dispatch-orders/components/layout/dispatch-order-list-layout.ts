/** Shared layout tokens for the dispatch orders list panel (replicate per module). */
export const dispatchOrderListPadding = {
  x: "px-4 md:px-6",
  toolbar: "px-4 py-3 md:px-6 md:py-4",
} as const;
